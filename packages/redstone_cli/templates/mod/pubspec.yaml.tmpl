name: {{project_name}}
description: {{description}}
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  ffi: ^2.1.0
  dart_mod_server:
    path: {{packages_path}}/dart_mod_server
  dart_mod_common:
    path: {{packages_path}}/dart_mod_common

dev_dependencies:
  redstone_test:
    path: {{packages_path}}/redstone_test

# Redstone configuration
redstone:
  minecraft_version: "{{minecraft_version}}"
  org: {{org}}
  author: {{author}}
