name: {{project_name}}_common
description: Shared types for {{project_name_title}} (client/server agnostic)
version: 1.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.5.0

dependencies:
  dart_mod_common:
    path: {{packages_path}}/dart_mod_common
