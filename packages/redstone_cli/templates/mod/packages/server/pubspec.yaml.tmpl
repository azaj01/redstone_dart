name: {{project_name}}_server
description: Server-side logic for {{project_name_title}}
version: 1.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.5.0

dependencies:
  ffi: ^2.1.0
  dart_mod_server:
    path: {{packages_path}}/dart_mod_server
  dart_mod_common:
    path: {{packages_path}}/dart_mod_common
  {{project_name}}_common:
    path: ../common
  minecraft_mcp:
    path: {{packages_path}}/minecraft_mcp

dev_dependencies:
  redstone_test:
    path: {{packages_path}}/redstone_test
